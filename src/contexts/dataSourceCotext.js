import { createContext, Component } from 'react';

export const dataSourceContext = createContext();

class dataSourceContextProvider extends Component {
    state = {
    }
    render() { 
        return (
            0
        );
    }
}
 
export default dataSourceContextProvider;